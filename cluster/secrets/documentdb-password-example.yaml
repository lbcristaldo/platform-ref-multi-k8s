# IMPORTANT: This is an EXAMPLE. Use SealedSecrets or external secret management in production.
# Generate password: openssl rand -base64 32
apiVersion: v1
kind: Secret
metadata:
  name: documentdb-admin-password
  namespace: crossplane-system
type: Opaque
stringData:
  password: "CHANGE_ME_GENERATE_SECURE_PASSWORD"
  # Example generation command:
  # kubectl create secret generic documentdb-admin-password \
  #   --from-literal=password=$(openssl rand -base64 32) \
  #   -n crossplane-system
